# nixpacks.toml

[phases.setup]
# Especificamos explícitamente las librerías del sistema y extensiones PHP
nixPkgs = [
    "php82",
    "php82Extensions.gd",        
    "php82Extensions.pdo",
    "php82Extensions.pdo_pgsql", 
    "php82Extensions.mbstring",
    "php82Extensions.xml",
    "php82Extensions.bcmath",
    "php82Extensions.curl",
    "php82Extensions.zip",
    "php82Extensions.intl",
    "php82Extensions.fileinfo",  
    "composer",
    "git",
    "libpng",                    # Dependencia de sistema para GD
    "libjpeg",                   # Dependencia de sistema para GD
    "freetype"                   # Dependencia de sistema para fuentes
]

[phases.build]
cmds = [
    "mkdir -p storage/framework/{sessions,views,cache}",
    "composer install --optimize-autoloader --no-dev --no-interaction --progress --ignore-platform-req=ext-gd",
    "php artisan config:cache",
    "php artisan route:cache",
    "php artisan view:cache"
]

[start]
cmd = "php artisan migrate --force && php artisan serve --host=0.0.0.0 --port=$PORT"